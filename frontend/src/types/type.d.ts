// src/types/type.d.ts
// ─────────────────────────────────────────────────────────
// 이 파일은 "앱 전체에서 함께 쓰는 데이터 형태(타입)"를 한곳에 정리한 선언 파일입니다.
//    - 기획/디자인/QA 동료도 각 데이터가 어떤 필드를 갖는지 빠르게 파악할 수 있도록
//      쉬운 한글 설명을 붙였습니다.
//    - 실제 화면/섹션/컴포넌트는 이 타입들을 기준으로 데이터를 주고받습니다.
//
// ▶ 용어 메모
//   • 타입(type): 데이터의 "형태"(필드 목록과 각 필드의 의미/종류)를 말합니다.
//   • 선택 필드(optional): 값이 없어도 되는 필드. 코드에서는 이름 뒤에 `?` 로 표시합니다.
//   • epoch ms(에포크 밀리초): 1970-01-01 00:00:00 UTC부터 지금까지 지난 시간(밀리초 단위) 숫자.
//                              날짜/시간을 정렬·비교할 때 많이 씁니다.
//
// ─────────────────────────────────────────────────────────
// RecordItem: "취향 기록" 카드 한 장의 데이터 구조
//  - 화면 예: BookCard(책 프레임 카드), RecordGallery(갤러리 그리드), TasteList(슬라이더)
//  - 정렬/필터: category로 필터링, createdAt으로 최신순 정렬(없으면 뒤로 밀림)
export type RecordItem = {
  id: string;            // 고유 식별자(주소/상세 진입 등에 사용). 예: "rec_001"
  title: string;         // 카드 제목. 예: "라라랜드"
  desc: string;          // 한 줄 설명(부제/요약). 예: "재즈와 꿈의 도시"
  thumb: string;         // 썸네일 이미지 경로/URL (없을 경우 기본 아이콘으로 대체 가능)
  category: string;      // 분류(필터). 예: "영화"/"음악"/"도서" 등
  createdAt?: number;    // (선택) 생성 시각(epoch ms). 최신순 정렬에 사용, 없으면 0 취급
  tags?: string[];       // (선택) 해시태그/키워드 모음. 예: ["#주말", "#친구"]
};

// ─────────────────────────────────────────────────────────
// OfficialDexItem: "공식 도감" 업적 카드의 데이터 구조
//  - 화면 예: OfficialDex 섹션의 WoodPlaque(액자 모양 카드)
//  - achieved가 true면 달성 뱃지/스타일로 표시, 값이 없거나 false면 미달성으로 표시
export type OfficialDexItem = {
  id: string;            // 고유 식별자. 예: "achv_202"
  title: string;         // 업적 제목. 예: "주말 연속 3회 기록"
  achieved?: boolean;    // (선택) 달성 여부(true/false). 값이 없으면 미달성으로 간주 가능
};

// ─────────────────────────────────────────────────────────
// PersonalChallenge: "개인 도감"의 진행도(Progress) 항목 데이터 구조
//  - 화면 예: PersonalDex 섹션(막대그래프로 % 진행 표시)
//  - progress는 0~1 사이 숫자 → 화면에서는 ×100 후 반올림하여 %로 표기합니다.
export type PersonalChallenge = {
  id: string;            // 고유 식별자. 예: "pc_01"
  title: string;         // 챌린지 이름. 예: "올해 12권 읽기"
  progress: number;      // 진행 비율(0~1). 예: 0.42 → 42%
};

// ─────────────────────────────────────────────────────────
// CategoryItem: 카테고리형 데이터가 RecordItem과 동일 구조일 때의 별칭(alias)
//  - 의미: "카테고리용 항목 = RecordItem과 같음"을 코드 레벨에서 명확히 표현
//  - 추후 카테고리 전용 필드가 생기면 별도 타입으로 분리할 수 있습니다.
export type CategoryItem = RecordItem;

// ─────────────────────────────────────────────────────────
// 🔎 참고/가이드
//  - 타입을 바꾸면 해당 타입을 사용하는 컴포넌트들(예: BookCard, OfficialDex 등)이
//    함께 수정되어야 할 수 있습니다. 필드 삭제/이름 변경은 특히 주의하세요.
//  - 선택 필드(`?`)는 데이터가 없어도 안전하게 렌더되도록 UI에서 조건부 처리를 권장합니다.
//  - 날짜/시간은 가능한 epoch ms(숫자)로 보관하고, 화면에 보일 때만 형식화(YYYY.MM.DD)하세요.
